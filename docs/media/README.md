# Media

Drop screenshots and GIFs referenced from the root [`README.md`](../../README.md#screenshots) here:

- `dashboard.png` — main overlay with all default widgets
- `tear-off.gif` — drag-to-detach gesture in action
- `forecast.png` — forecast widget close-up
- `themes.png` — MD3 vs Glass side-by-side

Recommended dimensions: 1080–1280 px wide, lossless PNG (or optimized GIF for animations). Keep individual files under ~2 MB to stay under GitHub's image-display limits and the README's load budget.
